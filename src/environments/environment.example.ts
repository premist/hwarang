export const environment = {
  production: false,
  firebase: {
    // Go to Firebase console, Settings,
    // and click 'Add Firebase to your web app',
    // and copy paste config object here
  }
}
